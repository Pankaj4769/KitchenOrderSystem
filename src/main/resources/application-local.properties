# =========================================================================
# LOCAL profile - developer workstation
# =========================================================================

#server.port=8082

# Local item image upload — relative to backend working directory; works on Mac/Linux/Windows
item.image.upload.path=${ITEM_IMAGE_UPLOAD_PATH:./uploads/item-images/}
item.image.public-base-url=${ITEM_IMAGE_PUBLIC_BASE_URL:http://localhost:8080/restaurant-images}

# Local DB - using shared Hostinger MySQL for now; switch to localhost when available
spring.datasource.url=jdbc:mysql://srv1825.hstgr.io:3306/u661381840_tech2kos?useSSL=false&allowPublicKeyRetrieval=true&connectTimeout=5000&socketTimeout=15000&autoReconnect=true
spring.datasource.username=u661381840_rootkos
spring.datasource.password=Rootkos@123

spring.jpa.show-sql=true

# Basic auth (dev convenience)
spring.security.user.name=pankaj
spring.security.user.password=123

# Mail - Hostinger SMTP
spring.mail.host=smtp.hostinger.com
spring.mail.port=587
spring.mail.username=noreply@tech2software.com
spring.mail.password=Tech2Noreply@2025

# Payments - mock locally
payment.gateway=mock

# Logs - local path
app.log.dir=/Users/itisasaw/Downloads/Logs
app.log.file=kos-app-local.log

# Verbose logging locally
logging.level.com.kos=DEBUG

# JWT secret — local dev default. Override with JWT_SECRET env var if you want.
# DO NOT use this value in dev/prod profiles.
app.jwt.secret=${JWT_SECRET:local-dev-only-secret-DO-NOT-USE-IN-ANY-OTHER-ENV-32b}
